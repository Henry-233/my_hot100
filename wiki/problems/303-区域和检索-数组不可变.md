---
title: "303. 区域和检索 - 数组不可变"
tags: [前缀和, 数组, easy]
difficulty: easy
status: todo
date_solved:
pattern: [[../patterns/phase1-数组/前缀和]]
---

## 题目描述
给定数组 nums，实现 NumArray 类，支持多次调用 sumRange(left, right)
返回闭区间 [left, right] 的元素和。数组不可变。

## 解题思路
一维前缀和裸题。构造 preSum 数组，preSum[i] = sum(nums[0..i-1])，
区间和 = preSum[right+1] - preSum[left]。

## 关键代码
```python
class NumArray:
    def __init__(self, nums):
        self.preSum = [0] * (len(nums) + 1)
        for i in range(len(nums)):
            self.preSum[i + 1] = self.preSum[i] + nums[i]

    def sumRange(self, left, right):
        return self.preSum[right + 1] - self.preSum[left]
```

## 复杂度
- 时间：构造 O(n)，查询 O(1)
- 空间：O(n)

## 关联
- Pattern: [[../patterns/phase1-数组/前缀和]]
- 相似题: [[304-二维区域和检索-矩阵不可变]]
- 易错点: preSum 长度为 n+1，preSum[0]=0 是哨兵
