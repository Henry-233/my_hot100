---
title: "循环数组"
tags: [数组, 循环, phase3]
phase: 3
status: not-started
depends_on: [[二维数组]]
---

## 适用场景
数组首尾相连形成环，需要处理环形遍历、环形子数组问题。
特征：看到"环形数组"、"环形子数组"、"循环队列"、
"环形链表数组版"、"轮转数组"。

## 代码模板
```python
# 环形遍历 — 取模法
def circular_traverse(arr, start, steps):
    n = len(arr)
    for i in range(steps):
        idx = (start + i) % n
        # 处理 arr[idx]

# 环形子数组最大和（拼接数组 + 前缀和 + 单调队列）
def max_subarray_sum_circular(nums):
    n = len(nums)
    # 拼接一次：处理跨边界情况
    extended = nums + nums
    prefix = [0]
    for x in extended:
        prefix.append(prefix[-1] + x)
    # 单调队列维护长度为 n 的窗口内最小前缀和
    from collections import deque
    q = deque([0])
    res = float('-inf')
    for i in range(1, len(prefix)):
        if q[0] < i - n:
            q.popleft()
        res = max(res, prefix[i] - prefix[q[0]])
        while q and prefix[q[-1]] >= prefix[i]:
            q.pop()
        q.append(i)
    return res
```

## 属于该模式的题目列表
- (待录入)

## 与相似模式的区别
- **二维数组**：循环数组是一维数组的环形变体
- **栈与队列**：循环数组常用于实现循环队列
- **滑动窗口**：循环子数组问题常转化为滑动窗口在拼接数组上
