---
title: "最短路径"
tags: [最短路径, 图, Dijkstra, Floyd, phase7]
phase: 7
status: not-started
depends_on: [[广度优先搜索], [图]]
---

## 适用场景
加权图上求两点间最短路径或全源最短路径。
特征：看到"最短路径（加权）"、"最小花费"、"网络延迟"、
"Dijkstra"、"Floyd-Warshall"。

## 代码模板
```python
import heapq

# Dijkstra — 单源最短路径（非负边权）
def dijkstra(graph, n, start):
    dist = [float('inf')] * n
    dist[start] = 0
    heap = [(0, start)]  # (距离, 节点)
    while heap:
        d, u = heapq.heappop(heap)
        if d > dist[u]:
            continue
        for v, w in graph[u]:
            nd = d + w
            if nd < dist[v]:
                dist[v] = nd
                heapq.heappush(heap, (nd, v))
    return dist
```

```python
# Floyd-Warshall — 全源最短路径
def floyd_warshall(n, edges):
    dist = [[float('inf')] * n for _ in range(n)]
    for i in range(n):
        dist[i][i] = 0
    for u, v, w in edges:
        dist[u][v] = w
    for k in range(n):
        for i in range(n):
            for j in range(n):
                dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j])
    return dist
```

## 属于该模式的题目列表
- (待录入)

## 与相似模式的区别
- **BFS/广度优先搜索**：BFS 处理无权图最短路径（O(V+E)），Dijkstra 处理带权图（O(E log V)）
- **动态规划**：Floyd-Warshall 本质是 DP，Bellman-Ford 也是 DP
- **DFS**：DFS 不适合求最短路径（会超时）
